# Core dependencies
redis[hiredis]==5.0.1  # Async support with C parser for performance
fastapi==0.104.0
uvicorn[standard]==0.24.0
pydantic==2.4.0
pydantic-settings==2.0.3

# Data processing
numpy>=1.26.0
# pandas>=2.2.0  # Legacy/offline analysis dependency, not part of active src deploy path

# Legacy data pipeline dependencies kept commented for future reuse
# google-cloud-bigquery==3.13.0
# google-auth==2.25.0
# db-dtypes==1.4.3
# clickhouse-driver>=0.2.6

# Background jobs
apscheduler==3.10.0

# Monitoring
prometheus-client==0.18.0
sentry-sdk[fastapi]>=2.0.0,<3.0.0

# Active API dependencies
httpx==0.25.0
aiohttp==3.9.1
ic-py==1.0.1
# python-multipart==0.0.6  # Not used by current src service

# Development and CI
pytest==7.4.3
pytest-asyncio==0.21.0
black==23.10.0
isort==5.12.0
dotenv

# Legacy/optional observability tooling
# pyinstrument
